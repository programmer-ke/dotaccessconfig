* dotaccessconfig

A configuration object that supports dot notation access e.g.
(`config.database.host`) while loading from dictionaries, JSON, or
yaml. Has support for nested sections that also become dot-accessible.

It is a drop-in replacement for `argparse.Namespace` or simple config
objects in CLI tools and web apps.
